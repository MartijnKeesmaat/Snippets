import React, { Component } from 'react';

class Header extends React.component {
  render() {
    return (
      <header>
        <a href="#">Log in</a>
        <a href="#">Log out</a>
        <a href="#">Sign up</a>
      </header>
    );
  }
}
