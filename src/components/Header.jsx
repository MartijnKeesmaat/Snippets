import React, { Component } from 'react';

class Header extends React.Component {
  render() {
    return (
      <header>
        <a href="#">Log in</a>
        <a href="#">Log out</a>
        <a href="#">Sign up</a>
      </header>
    );
  }
}

export default Header;
